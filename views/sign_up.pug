doctype html
html
  head
    title  Sign Up Page
    link(rel='stylesheet', href='/../css/signform.css')
  body
    h1#pageTitle 使用者註冊
    #signUpBlock
      h2 新增使用者資訊
      form#signUpForm 
        div
          label
            | 使用者名稱 (name)
            br
            input#name(type='text', name='name', required='required')
        div
          label
            | 電子郵件信箱 (e-mail address)
            br
            input#email(type='text', name='email', required='required')
        div
          label
            | 密碼 (password)
            br
            input#password(type='text', name='password', required='required')
        div
          label
            | 照片 (picture)
            br
            input#mainImage(type='file', name='picture')
        div
          button.signButton(type='submit', value='註冊') 註冊
    
  footer
    div
      p#registerResponse
    script(src='/../js/signup.js')
    //- block res_msg
